run = "python -m uvicorn app:app --host 0.0.0.0 --port 8000"
language = "python3"
entrypoint = "app.py"

[nix]
channel = "stable-23_11"

[deployment]
run = ["sh", "-c", "python -m uvicorn app:app --host 0.0.0.0 --port 8000"]
